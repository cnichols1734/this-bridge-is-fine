{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "python -u -m backend.ingest --resume",
    "healthcheckPath": null,
    "healthcheckTimeout": null,
    "restartPolicyType": "NEVER"
  }
}
